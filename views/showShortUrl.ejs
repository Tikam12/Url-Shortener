<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Short URL</title>
</head>
<body class="bg-gradient-to-r from-green-400 via-blue-500 to-purple-600 min-h-screen flex flex-col items-center justify-center p-8">

    <!-- Header -->
    <header class="text-center mb-10">
        <h1 class="text-4xl font-bold text-white mb-4">Short URL Generator</h1>
        <p class="text-lg text-white opacity-90">Create and manage your shortened links effortlessly!</p>
    </header>

    <!-- Shortened URL Card -->
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg w-full text-center card-hover">
        <h2 class="text-3xl font-bold text-blue-600 mb-4">Your Short URL is Ready!</h2>

        <!-- Original URL -->
        <div class="mb-4">
            <p class="text-gray-700 font-semibold">Original URL: <span class="text-blue-500"><%= url.redirectURL %></span></p>
            <!-- <div class="bg-gray-100 p-4 rounded-lg border border-gray-300 text-gray-800 text-sm font-mono truncate"> -->
                <!-- <%= url.redirectURL %> -->
            <!-- </div> -->
        </div>

        <!-- Shortened URL -->
        <div class="mb-6">
            <p class="text-gray-700 font-semibold">Short URL:</p>
            <div class="bg-gray-100 p-4 rounded-lg border border-gray-300 text-gray-800 text-sm font-mono font-semibold">
                <%= shortUrl %>
            </div>
        </div>

        <!-- Copy Button -->
        <button id="copyButton"
            class="w-full py-3 bg-blue-500 text-white font-semibold rounded-lg shadow-lg hover:bg-blue-600 hover:scale-105 transition transform duration-200 ease-in-out focus:outline-none">
            Copy Link
        </button>
    </div>

    <!-- "View All URLs" Link -->
    <div class="mt-8">
        <a href="/showAllUrls"
            class="py-3 px-6 bg-purple-500 text-white font-semibold rounded-lg shadow-lg hover:bg-purple-600 hover:scale-105 transition transform duration-200 ease-in-out focus:outline-none">
            View All URLs
        </a>
    </div>

    <!-- Footer -->
    <footer class="mt-12 text-center">
        <p class="text-white opacity-80">Made with ❤️ for efficient URL management</p>
    </footer>

    <script>
        const copyButton = document.getElementById('copyButton');
        const shortUrl = "<%= shortUrl %>";

        copyButton.addEventListener('click', () => {
            navigator.clipboard.writeText(shortUrl)
            .then(() => { alert('Copied to clipboard!') })
            .catch(() => { alert('Failed to copy!') });
        });

    </script>

</body>

</html>
